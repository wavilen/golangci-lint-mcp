# staticcheck: S1013

<instructions>
Detects code that manually computes seek offsets using arithmetic instead of using the `io.SeekStart`, `io.SeekCurrent`, and `io.SeekEnd` constants. These constants were introduced in Go 1.7 to replace magic numbers.

Use the named constants from the `io` package for seek whence values.
</instructions>

<examples>
## Bad
```go
file.Seek(0, 2) // seek to end
```

## Good
```go
file.Seek(0, io.SeekEnd)
```
</examples>

<patterns>
- Using magic numbers 0, 1, 2 for seek whence values
- Computing seek position manually instead of using constants
- Using os.SEEK_SET/SEEK_CUR/SEEK_END (deprecated aliases)
- Hard-coded offset constants in Seek calls
</patterns>

<related>
S1015, S1030, SA1013
