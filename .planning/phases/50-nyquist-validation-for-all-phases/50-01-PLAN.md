---
phase: 50-nyquist-validation-for-all-phases
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/31-opencode-plugin-command/31-VALIDATION.md
  - .planning/phases/32-platform-rules-files/32-VALIDATION.md
  - .planning/phases/33-claude-code-hooks/33-VALIDATION.md
  - .planning/phases/34-golden-config-pipeline/34-VALIDATION.md
  - .planning/phases/35-fix-guide-violations/35-VALIDATION.md
  - .planning/phases/36-prepare-commands-golangci-lint-md-with-llm-prompting-best-pr/36-VALIDATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every phase 31-36 has a VALIDATION.md file in its planning directory"
    - "Each VALIDATION.md has correct nyquist_compliant status (honest assessment)"
    - "Per-task verification maps accurately reflect actual test coverage from each phase"
    - "Manual-only verifications explain WHY automation is not possible"
    - "Phases with existing test infrastructure (make crosscheck) are correctly identified"
  artifacts:
    - path: ".planning/phases/31-opencode-plugin-command/31-VALIDATION.md"
      provides: "Nyquist validation contract for opencode plugin phase"
      contains: "nyquist_compliant"
    - path: ".planning/phases/32-platform-rules-files/32-VALIDATION.md"
      provides: "Nyquist validation contract for rules files phase"
      contains: "nyquist_compliant"
    - path: ".planning/phases/33-claude-code-hooks/33-VALIDATION.md"
      provides: "Nyquist validation contract for Claude Code hooks phase"
      contains: "nyquist_compliant"
    - path: ".planning/phases/34-golden-config-pipeline/34-VALIDATION.md"
      provides: "Nyquist validation contract for golden config pipeline phase"
      contains: "nyquist_compliant"
    - path: ".planning/phases/35-fix-guide-violations/35-VALIDATION.md"
      provides: "Nyquist validation contract for guide fix phase"
      contains: "nyquist_compliant"
    - path: ".planning/phases/36-prepare-commands-golangci-lint-md-with-llm-prompting-best-pr/36-VALIDATION.md"
      provides: "Nyquist validation contract for prompt rewrite phase"
      contains: "nyquist_compliant"
  key_links:
    - from: "50-RESEARCH.md Per-Phase Task Maps"
      to: "31-VALIDATION.md through 36-VALIDATION.md"
      via: "per-phase classification drives VALIDATION.md content"
      pattern: "Test Infrastructure"
---

<objective>
Create VALIDATION.md files for phases 31-36 (platform integration phases), establishing Nyquist sampling contracts for each.

Purpose: None of the 14 v1.1 phases have VALIDATION.md files. This plan creates them for the first 6 phases, following the GSD template from `$HOME/.config/opencode/get-shit-done/templates/VALIDATION.md` and using the per-phase classification data from `50-RESEARCH.md`.

Output: 6 VALIDATION.md files in their respective phase directories.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-nyquist-validation-for-all-phases/50-RESEARCH.md

<interfaces>
<!-- VALIDATION.md template structure from $HOME/.config/opencode/get-shit-done/templates/VALIDATION.md -->

Required frontmatter fields (YAML between triple-dash markers):
  phase: {N} | slug: {phase-slug} | status: draft | nyquist_compliant: false | wave_0_complete: false | created: {date}

Required sections (in order):
1. `# Phase {N} — Validation Strategy`
2. `## Test Infrastructure` — table with Framework, Config file, Quick run command, Full suite command, Estimated runtime
3. `## Sampling Rate` — per-task and per-wave cadences
4. `## Per-task Verification Map` — table with columns: task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status
5. `## Wave 0 Requirements` — test stubs needed (or "Existing infrastructure covers all phase requirements")
6. `## Manual-Only Verifications` — table with Behavior | Requirement | Why Manual | Test Instructions
7. `## Validation Sign-Off` — checklist

Status symbols: pending | green | red | flaky (use emoji: ⬜ ✅ ❌ ⚠️)
Test types: unit | integration | smoke | e2e | manual-only | structural
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Create VALIDATION.md for phases 31, 32, 33 (platform integration)</name>
  <files>
    .planning/phases/31-opencode-plugin-command/31-VALIDATION.md,
    .planning/phases/32-platform-rules-files/32-VALIDATION.md,
    .planning/phases/33-claude-code-hooks/33-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/50-nyquist-validation-for-all-phases/50-RESEARCH.md (per-phase task maps — PRIMARY SOURCE)
    - .planning/phases/31-opencode-plugin-command/31-01-PLAN.md (task structure)
    - .planning/phases/31-opencode-plugin-command/31-01-SUMMARY.md (what was actually done)
    - .planning/phases/31-opencode-plugin-command/31-VERIFICATION.md (existing verification results)
    - .planning/phases/32-platform-rules-files/32-01-PLAN.md (task structure)
    - .planning/phases/32-platform-rules-files/32-01-SUMMARY.md (what was actually done)
    - .planning/phases/32-platform-rules-files/32-VERIFICATION.md (existing verification results)
    - .planning/phases/33-claude-code-hooks/33-01-PLAN.md (task structure)
    - .planning/phases/33-claude-code-hooks/33-01-SUMMARY.md (what was actually done)
    - .planning/phases/33-claude-code-hooks/33-VERIFICATION.md (existing verification results)
  </read_first>
  <action>
Create 3 VALIDATION.md files for phases 31, 32, and 33. Use the standard GSD template (see interfaces block above). For each file, follow this process:

**Phase 31: opencode Plugin & Command**
- Read 31-01-PLAN.md to extract task IDs (31-01-T1 for command template, 31-01-T2 for installer)
- Set `nyquist_compliant: false` — both tasks are manual-only (command template is a prompt, installer writes to ~/.config)
- Requirements: OPLG-01, OPLG-02, OPLG-03, OPLG-04, OCMD-01
- Test Infrastructure: N/A — documentation-only phase; quick run command: `grep -c "golangci_lint_parse" commands/golangci-lint.md`
- Per-task map:
  - 31-01-T1 (command template): Test Type = manual-only, no automated command
  - 31-01-T2 (installer): Test Type = manual-only, no automated command; structural check: `grep '"commands/"' package.json`
- Manual-only verifications: Prompt quality (agent interprets command correctly), installer writes to correct paths
- Slug: `opencode-plugin-command`
- Status: `compliant` (phase completed, validation retroactive)

**Phase 32: Platform Rules Files**
- Read 32-01-PLAN.md to extract task IDs (32-01-T1 for rules files, 32-01-T2 for installer extension)
- Set `nyquist_compliant: false` — installer task is manual-only; rules file quality is semantic
- Requirements: RULE-01, RULE-02, RULE-03
- Test Infrastructure: N/A; quick run: `test -f rules/claude-code.md && test -f rules/cursor.mdc && test -f rules/opencode.md`
- Per-task map:
  - 32-01-T1 (rules files): Test Type = structural, command: `test -f rules/claude-code.md && grep -q "golangci_lint_parse" rules/claude-code.md`
  - 32-01-T2 (installer): Test Type = manual-only, structural check: `grep '"rules/"' package.json`
- Manual-only verifications: Rule content quality, platform detection correctness
- Slug: `platform-rules-files`
- Status: `compliant`

**Phase 33: Claude Code Hooks**
- Read 33-01-PLAN.md to extract task IDs (33-01-T1 for hook script, 33-01-T2 for installer extension)
- Set `nyquist_compliant: false` — hooks only work within Claude Code runtime
- Requirements: CCHOK-01, CCHOK-02, CCHOK-03
- Test Infrastructure: N/A; quick run: `node -c hooks/golangci-lint-post.js` (syntax check)
- Per-task map:
  - 33-01-T1 (hook script): Test Type = manual-only, structural: `node -c hooks/golangci-lint-post.js`
  - 33-01-T2 (installer): Test Type = manual-only, structural: `grep -q 'hooks/' package.json`
- Manual-only verifications: Hook behavior in Claude Code runtime, settings.json merge correctness, graceful exit on all error paths
- Slug: `claude-code-hooks`
- Status: `compliant`

For each file, set `wave_0_complete: true` (no new test stubs needed — phases are completed). Set `created: 2026-04-22`.

**VALIDATION.md ≠ VERIFICATION.md rule:** Do NOT include "Observable Truths" tables or execution review content. VALIDATION.md is the Nyquist sampling contract — it maps tasks to test coverage, not execution outcomes.
  </action>
  <verify>
    <automated>for f in 31 32 33; do
      case $f in
        31) dir="31-opencode-plugin-command" ;;
        32) dir="32-platform-rules-files" ;;
        33) dir="33-claude-code-hooks" ;;
      esac
      FILE=".planning/phases/$dir/$f-VALIDATION.md"
      test -f "$FILE" || { echo "MISSING: $FILE"; exit 1; }
      grep -q "nyquist_compliant" "$FILE" || { echo "MISSING nyquist_compliant in $FILE"; exit 1; }
      grep -q "Per-task Verification Map" "$FILE" || { echo "MISSING per-task map in $FILE"; exit 1; }
      grep -q "Manual-Only Verifications" "$FILE" || { echo "MISSING manual-only in $FILE"; exit 1; }
      grep -q "Validation Sign-Off" "$FILE" || { echo "MISSING sign-off in $FILE"; exit 1; }
    done && echo "PASS: All 3 VALIDATION.md files exist with required sections"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/31-opencode-plugin-command/31-VALIDATION.md` exists with frontmatter containing `nyquist_compliant: false`
    - `.planning/phases/32-platform-rules-files/32-VALIDATION.md` exists with frontmatter containing `nyquist_compliant: false`
    - `.planning/phases/33-claude-code-hooks/33-VALIDATION.md` exists with frontmatter containing `nyquist_compliant: false`
    - Each file contains sections: Test Infrastructure, Sampling Rate, Per-task Verification Map, Manual-Only Verifications, Validation Sign-Off
    - Phase 31 file references requirements OPLG-01 through OPLG-04 and OCMD-01
    - Phase 32 file references requirements RULE-01 through RULE-03
    - Phase 33 file references requirements CCHOK-01 through CCHOK-03
    - Phase 31 has 2 rows in per-task map (31-01-T1, 31-01-T2)
    - Phase 32 has 2 rows in per-task map (32-01-T1, 32-01-T2)
    - Phase 33 has 2 rows in per-task map (33-01-T1, 33-01-T2)
    - No file contains "Observable Truths" (that is VERIFICATION.md territory)
  </acceptance_criteria>
  <done>3 VALIDATION.md files created for phases 31, 32, 33 with honest nyquist_compliant: false assessments, per-task maps, and manual-only verification tables.</done>
</task>

<task type="auto">
  <name>task 2: Create VALIDATION.md for phases 34, 35, 36 (pipeline + prompt phases)</name>
  <files>
    .planning/phases/34-golden-config-pipeline/34-VALIDATION.md,
    .planning/phases/35-fix-guide-violations/35-VALIDATION.md,
    .planning/phases/36-prepare-commands-golangci-lint-md-with-llm-prompting-best-pr/36-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/50-nyquist-validation-for-all-phases/50-RESEARCH.md (per-phase task maps — PRIMARY SOURCE)
    - .planning/phases/34-golden-config-pipeline/34-01-PLAN.md (task structure for plan 01)
    - .planning/phases/34-golden-config-pipeline/34-02-PLAN.md (task structure for plan 02)
    - .planning/phases/34-golden-config-pipeline/34-VERIFICATION.md (existing verification results)
    - .planning/phases/35-fix-guide-violations/35-01-PLAN.md (task structure)
    - .planning/phases/35-fix-guide-violations/35-01-SUMMARY.md (what was done)
    - .planning/phases/36-prepare-commands-golangci-lint-md-with-llm-prompting-best-pr/36-01-PLAN.md (task structure)
    - .planning/phases/36-prepare-commands-golangci-lint-md-with-llm-prompting-best-pr/36-01-SUMMARY.md (what was done)
  </read_first>
  <action>
Create 3 VALIDATION.md files for phases 34, 35, and 36. Use the standard GSD template.

**Phase 34: Golden Config Pipeline (2 plans)**
- Read 34-01-PLAN.md and 34-02-PLAN.md to extract task IDs
- Set `nyquist_compliant: false` — while `make crosscheck` provides integration coverage for the pipeline, the golden config vendoring task is structural-only
- Requirements: XCHK-01, XCHK-02, XCHK-03, XCHK-04
- Test Infrastructure: Go testing + make crosscheck; Quick run: `test -f golden-config/.golangci.yml`; Full suite: `make crosscheck`
- Per-task map:
  - 34-01-T1 (vendor golden config): Test Type = structural, command: `test -f golden-config/.golangci.yml && grep -q "linters:" golden-config/.golangci.yml`
  - 34-02-T1 (build pipeline): Test Type = integration, command: `make crosscheck`
  - 34-02-T2 (Makefile target + validate): Test Type = integration, command: `make crosscheck && python3 -c "import json; json.load(open('tmp/crosscheck/violations.json'))"`
- Sampling rate: After every task commit: `test -f golden-config/.golangci.yml`; After every plan wave: `make crosscheck`
- Slug: `golden-config-pipeline`
- Status: `compliant`

**Phase 35: Fix Guide Violations**
- Read 35-01-PLAN.md to extract task IDs
- Set `nyquist_compliant: false` — while `make crosscheck` validates the outcome, the guide fix task itself is manual (fixing markdown content)
- Requirements: XCHK-05
- Test Infrastructure: make crosscheck; Quick run: `make crosscheck`; Full suite: `make crosscheck`
- Per-task map:
  - 35-01-T1 (fix 2 guides): Test Type = integration, command: `make crosscheck` (validates fixes)
  - 35-01-T2 (verify crosscheck): Test Type = integration, command: `python3 -c "import json; r=json.load(open('tmp/crosscheck/violations.json')); assert r['failing']==0"`
- Slug: `fix-guide-violations`
- Status: `compliant`

**Phase 36: Prepare commands/golangci-lint.md**
- Read 36-01-PLAN.md to extract task IDs (36-01-T1 for rewrite, 36-01-T2 for validation)
- Set `nyquist_compliant: false` — prompt rewrite quality is semantic, not automatable
- Requirements: None tracked (documentation-only)
- Test Infrastructure: N/A — documentation-only phase; Quick run: `grep -c "Strategy" commands/golangci-lint.md`
- Per-task map:
  - 36-01-T1 (rewrite command): Test Type = manual-only, structural: `wc -l commands/golangci-lint.md | grep -E '1[8-9][0-9]|2[0-7][0-9]|280'`
  - 36-01-T2 (validate + lint): Test Type = manual-only, structural: `golangci-lint run ./...`
- Manual-only verifications: Prompt quality (semantic evaluation of 11 prompting techniques), agent interpretation correctness
- Slug: `prepare-commands-golangci-lint-md-with-llm-prompting-best-pr`
- Status: `compliant`

For each file, set `wave_0_complete: true` and `created: 2026-04-22`.

**IMPORTANT:** Do NOT confuse VALIDATION.md with VERIFICATION.md. VALIDATION.md maps tasks to test coverage (Nyquist sampling contract). Do NOT include "Observable Truths" tables.
  </action>
  <verify>
    <automated>for f in 34 35 36; do
      case $f in
        34) dir="34-golden-config-pipeline" ;;
        35) dir="35-fix-guide-violations" ;;
        36) dir="36-prepare-commands-golangci-lint-md-with-llm-prompting-best-pr" ;;
      esac
      FILE=".planning/phases/$dir/$f-VALIDATION.md"
      test -f "$FILE" || { echo "MISSING: $FILE"; exit 1; }
      grep -q "nyquist_compliant" "$FILE" || { echo "MISSING nyquist_compliant in $FILE"; exit 1; }
      grep -q "Per-task Verification Map" "$FILE" || { echo "MISSING per-task map in $FILE"; exit 1; }
      grep -q "Validation Sign-Off" "$FILE" || { echo "MISSING sign-off in $FILE"; exit 1; }
    done && echo "PASS: All 3 VALIDATION.md files exist with required sections"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/34-golden-config-pipeline/34-VALIDATION.md` exists with `nyquist_compliant: false`
    - `.planning/phases/35-fix-guide-violations/35-VALIDATION.md` exists with `nyquist_compliant: false`
    - `.planning/phases/36-prepare-commands-golangci-lint-md-with-llm-prompting-best-pr/36-VALIDATION.md` exists with `nyquist_compliant: false`
    - Phase 34 file references requirements XCHK-01 through XCHK-04
    - Phase 34 file has 3 rows in per-task map (34-01-T1, 34-02-T1, 34-02-T2)
    - Phase 34 file references `make crosscheck` as integration test command
    - Phase 35 file references requirement XCHK-05
    - Phase 35 file references `make crosscheck` as integration test command
    - Phase 36 file has manual-only classification for both tasks
    - No file contains "Observable Truths"
  </acceptance_criteria>
  <done>3 VALIDATION.md files created for phases 34, 35, 36 with honest nyquist_compliant: false assessments, integration test references for pipeline phases, and manual-only classifications for prompt phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VALIDATION.md → gsd-audit-milestone | Validation files consumed by audit workflow — honest assessments prevent false compliance claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | S | nyquist_compliant flag | mitigate | Honest false assessments — only 2/14 phases (39, 42) have true automated coverage |
| T-50-02 | I | Manual-only rationale | accept | Documenting why automation is infeasible is more valuable than fake structural tests |

</threat_model>

<verification>
1. All 6 VALIDATION.md files exist in their phase directories
2. Each file has valid YAML frontmatter with `phase`, `slug`, `status`, `nyquist_compliant`, `wave_0_complete`, `created`
3. Each file has all required sections (Test Infrastructure, Sampling Rate, Per-task Map, Manual-Only, Sign-Off)
4. No nyquist_compliant is set to `true` in this plan (phases 31-36 are all documentation/manual-only)
5. Each per-task map has correct number of rows matching the phase's actual plan tasks
</verification>

<success_criteria>
- 6 VALIDATION.md files created for phases 31-36
- All have honest `nyquist_compliant: false` assessments
- Per-task verification maps match actual task structure from PLAN files
- Manual-only verifications explain why automation is infeasible
- Template structure matches GSD VALIDATION.md template
</success_criteria>

<output>
After completion, create `.planning/phases/50-nyquist-validation-for-all-phases/50-01-SUMMARY.md`
</output>
