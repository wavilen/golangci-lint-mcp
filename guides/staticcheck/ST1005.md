# staticcheck: ST1005

<instructions>
Detects error strings that end with punctuation (periods, colons, exclamation marks, or newlines). Go convention says error strings should be standalone phrases without trailing punctuation, because they are often combined with context.

Remove trailing punctuation from error messages.
</instructions>

<examples>
## Bad
```go
return errors.New("file not found.")
```

## Good
```go
return errors.New("file not found")
```
</examples>

<patterns>
- Remove trailing periods from error messages
- Remove trailing colons from error messages
- Remove trailing exclamation marks from error messages
- Remove trailing punctuation that breaks error messages when wrapped with context
</patterns>

<related>
ST1004, errname, S1028
