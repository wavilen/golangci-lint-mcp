# staticcheck: S1025

<instructions>
Detects hardcoded time format strings that duplicate one of Go's standard time format constants. Go uses a unique reference time (Mon Jan 2 15:04:05 MST 2006) for formatting, and the standard library provides constants for common layouts.

Use the `time` package constants like `time.RFC3339`, `time.DateOnly`, or `time.DateTime` instead of hardcoding format strings.
</instructions>

<examples>
## Good
```go
formatted := t.Format(time.RFC3339)
```
</examples>

<patterns>
- Replace hardcoded `2006-01-02` with `time.DateOnly`
- Replace hardcoded `2006-01-02 15:04:05` with `time.DateTime`
- Replace hardcoded RFC3339 format strings with `time.RFC3339`
- Use `time` package constants for any custom format that matches a standard layout
</patterns>

<related>
staticcheck/S1012, staticcheck/S1024, staticcheck/SA1005
</related>
