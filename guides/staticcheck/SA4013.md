# staticcheck: SA4013

<instructions>
Detects assignments to `err` where the previous error value is discarded without being checked. This means the first error is silently swallowed, which can hide failures. Each assignment to `err` should be preceded by handling the previous value.

Check or return the previous error before overwriting it. Use `err = errors.Join(err, newErr)` to accumulate errors, or use a different variable name.
</instructions>

<examples>
## Bad
```go
_, err = os.Stat(path1)
_, err = os.Stat(path2) // path1 error lost
if err != nil {
    return err
}
```

## Good
```go
if _, err := os.Stat(path1); err != nil {
    return err
}
if _, err := os.Stat(path2); err != nil {
    return err
}
```
</examples>

<patterns>
- Multiple error-returning calls reusing the same `err` variable
- `err` overwritten in cleanup blocks, hiding the original error
- Sequential operations where only the last error is checked
- Missing early return after detecting an error
</patterns>

<related>
errcheck, SA5001
</related>
