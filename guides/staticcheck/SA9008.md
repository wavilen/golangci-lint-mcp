# staticcheck: SA9008

<instructions>
Detects `return` statements inside deferred functions. A `return` inside a deferred function suppresses the panic recovery mechanism and does not return a value to the caller. It also makes the control flow confusing — the deferred function's return value is discarded.

Avoid `return` in deferred functions. If you need to recover from a panic, use `recover()` and set named return values. If you need to modify the return value, use named returns.
</instructions>

<examples>
## Good
```go
func safeRun() (err error) {
    defer func() {
        if r := recover(); r != nil {
            err = fmt.Errorf("panic: %v", r)
        }
    }()
    doWork()
    return nil
}
```
</examples>

<patterns>
- Avoid `return` inside `defer func()` blocks — set named return values instead
- Use `recover()` with named return values instead of `return` to handle panics in deferred functions
- Avoid deferred returns that override function returns — use named returns for modification
- Set named return values correctly in deferred functions instead of using bare `return`
</patterns>

<related>
staticcheck/SA9001, staticcheck/SA5003
</related>
