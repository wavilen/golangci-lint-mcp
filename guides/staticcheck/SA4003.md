# staticcheck: SA4003

<instructions>
Detects comparisons of the form `x >= 0` or `x > 0` (with unsigned types, depending on context) where the comparison is always true due to the unsigned type. This is the complement of SA4001 — unsigned values are never negative, so `>= 0` is trivially true.

Remove the redundant comparison or use a signed integer type if negative values need to be tracked.
</instructions>

<examples>
## Good
```go
var count uint
if count > 0 {
    slog.Info("has items")
}
```
</examples>

<patterns>
- Remove `uint >= 0` comparisons — always true for unsigned types
- Remove `uintptr >= 0` comparisons — trivially true
- Remove redundant zero-bound guard checks on unsigned loop counters
- Remove redundant `>= 0` checks on unsigned values
</patterns>

<related>
staticcheck/SA4001, staticcheck/SA4014
</related>
