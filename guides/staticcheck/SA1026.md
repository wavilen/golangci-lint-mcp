# staticcheck: SA1026

<instructions>
Detects `net.Listen` called with a service name (e.g., `"http"`, `"smtp"`) instead of a numeric port. Service names require a lookup in `/etc/services` and may not exist on all systems. Use a numeric port string like `":8080"` for reliability.
</instructions>

<examples>
## Bad
```go
ln, err := net.Listen("tcp", ":http") // relies on /etc/services lookup
```

## Good
```go
ln, err := net.Listen("tcp", ":8080")
```
</examples>

<patterns>
- Using well-known service names (`"http"`, `"https"`, `"ssh"`) in `net.Listen`
- Service name lookups failing on minimal containers or non-Unix systems
- Hardcoding service names that map to unexpected ports in custom environments
</patterns>

<related>
SA1028
</related>
