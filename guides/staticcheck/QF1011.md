# staticcheck: QF1011

<instructions>
Detects code that uses deprecated or older API patterns where a modern equivalent exists. This includes using `io/ioutil` (deprecated since Go 1.16), old `context` patterns, or superseded standard library functions.

Replace the deprecated API with its modern equivalent.
</instructions>

<examples>
## Bad
```go
data, err := ioutil.ReadFile("config.json")
```

## Good
```go
data, err := os.ReadFile("config.json")
```
</examples>

<patterns>
- Using io/ioutil.ReadFile → use os.ReadFile
- Using io/ioutil.WriteFile → use os.WriteFile
- Using io/ioutil.TempDir → use os.MkdirTemp
- Using io/ioutil.TempFile → use os.CreateTemp
</patterns>

<related>
ST1019, SA1019, S1013
