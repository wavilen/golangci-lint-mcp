# err113

<instructions>
Err113 (errcheck-style rule from go-err113) detects runtime errors created with `errors.New` or `fmt.Errorf` at package or function level, and checks that exported errors follow Go conventions. It also flags direct comparisons of errors with `==` instead of using `errors.Is`.

Define sentinel errors as package-level `var` values and use `errors.Is` for comparison.
</instructions>

<examples>
## Bad
```go
if err == io.EOF {
    return ""
}
```

## Good
```go
if errors.Is(err, io.EOF) {
    return ""
}
```
</examples>

<patterns>
- Direct `err == someError` comparisons that break error wrapping
- `errors.New()` called inside functions instead of at package level
- Unexported sentinel errors used across packages
- Missing `%w` verb when wrapping errors with `fmt.Errorf`
</patterns>

<related>
errcheck, wrapcheck, govet
