# staticcheck: ST1011

<instructions>
Detects inconsistent variable naming within a function or package. This includes using different naming styles for similar variables (e.g., mixing `userID` and `user_id`), or using abbreviations inconsistently.

Use consistent naming conventions throughout the codebase.
</instructions>

<examples>
## Bad
```go
func process(userId int, userName string) {
    user_id := getUserId(userId)
}
```

## Good
```go
func process(userID int, userName string) {
    id := getUserID(userID)
}
```
</examples>

<patterns>
- Mixing camelCase and snake_case for variable names
- Using different abbreviations for the same concept (id vs ID vs Id)
- Inconsistent naming across functions in the same package
- Variable names that shadow package names
</patterns>

<related>
ST1003, ST1006, ST1007
