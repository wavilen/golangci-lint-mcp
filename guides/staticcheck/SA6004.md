# staticcheck: SA6004

<instructions>
Detects string comparison with `==` on `[]byte` values without conversion. Comparing byte slices with string literals requires an implicit conversion each time, which allocates. For repeated comparisons, convert the byte slice to a string once.

Convert `[]byte` to `string` once before comparing, or use `bytes.Equal` for byte-slice-to-byte-slice comparisons.
</instructions>

<examples>
## Bad
```go
data := []byte("hello")
if string(data) == "hello" { // allocates on every call
    slog.Info("match")
}
```

## Good
```go
data := []byte("hello")
if bytes.Equal(data, []byte("hello")) {
    slog.Info("match")
}
```
</examples>

<patterns>
- Use `bytes.Equal` instead of `string(byteSlice) == "literal"` in hot paths
- Convert `[]byte` to `string` once before comparing in loops
- Use `bytes.Equal` for HTTP handler body comparison with literals
- Cache byte-to-string conversions instead of repeating them in loops
</patterns>

<related>
SA6005, SA6003
</related>
