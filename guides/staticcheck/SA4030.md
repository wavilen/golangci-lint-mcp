# staticcheck: SA4030

<instructions>
Detects use of the deprecated `strings.Title` function. `strings.Title` does not handle Unicode properly and does not follow title-casing rules for all languages. It has been deprecated since Go 1.18.

Replace with `cases.Title` from the `golang.org/x/text/cases` package, which handles Unicode correctly.
</instructions>

<examples>
## Good
```go
import "golang.org/x/text/cases"
import "golang.org/x/text/language"

title := cases.Title(language.English).String("hello world")
```
</examples>

<patterns>
- Replace `strings.Title` with `cases.Title` from `golang.org/x/text/cases`
- Import `golang.org/x/text/cases` and `golang.org/x/text/language` for proper title-casing
- Update pre-Go 1.18 code that uses the deprecated `strings.Title`
- Use `cases.Title` for Unicode-correct title-casing of user-facing text
</patterns>

<related>
staticcheck/SA1019, staticcheck/SA5005
</related>
