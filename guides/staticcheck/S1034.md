# staticcheck: S1034

<instructions>
Detects compound boolean expressions that can be simplified using De Morgan's laws or other boolean algebra. This includes `!a && !b` → `!(a || b)`, double negations `!!x` → `x`, and other simplifiable boolean expressions.

Simplify the boolean expression to its minimal form.
</instructions>

<examples>
## Bad
```go
if !(a != b) {
    slog.Info("equal")
}
```

## Good
```go
if a == b {
    slog.Info("equal")
}
```
</examples>

<patterns>
- Simplify `!!x` to `x` by removing double negation
- Replace `!(a != b)` with `a == b`
- Replace `!(a == b)` with `a != b`
- Apply De Morgan law to simplify complex `&&`/`||` chains
</patterns>

<related>
QF1001, S1008, gocritic/boolExprSimplify
