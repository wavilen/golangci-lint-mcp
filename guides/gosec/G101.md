# G101

<instructions>
Detects hardcoded credentials such as passwords, API keys, tokens, and secrets in source code. Embedding secrets in code makes them visible in version control, CI logs, and compiled binaries.

Move secrets to environment variables, secret managers, or configuration files outside the source tree. Use `os.Getenv` or a secrets management library.
</instructions>

<examples>
## Bad
```go
var password = "super-secret-123"

func connect() {
    db, _ := sql.Open("postgres", "postgres://user:"+password+"@db/host")
}
```

## Good
```go
func connect() {
    password := os.Getenv("DB_PASSWORD")
    if password == "" {
        log.Fatal("DB_PASSWORD not set")
    }
    db, _ := sql.Open("postgres", fmt.Sprintf("postgres://user:%s@db/host", password))
}
```
</examples>

<patterns>
- Hardcoded string values assigned to variables named password, secret, token, apiKey, privateKey
- Credentials embedded in connection strings or URLs
- API keys committed directly in source files
- Test fixtures containing real credentials instead of mock values
</patterns>

<related>
G304, G117, G407
</related>
