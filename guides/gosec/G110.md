# G110

<instructions>
Detects `io.Copy` used when decompressing data from a potentially malicious source. An attacker can craft compressed data that decompresses to a much larger size (zip bomb), causing memory exhaustion or disk fill.

Use `io.CopyN` with a reasonable size limit, or use `io.LimitedReader` to cap the decompressed output size.
</instructions>

<examples>
## Good
```go
gz, _ := gzip.NewReader(resp.Body)
limited := io.LimitReader(gz, 10*1024*1024) // cap at 10MB
io.Copy(dst, limited)
```
</examples>

<patterns>
- Limit `io.Copy` with decompression using `io.LimitReader` to cap maximum output size
- Set maximum decompressed size limits when processing uploaded archives — reject oversized payloads
- Wrap HTTP response body reads with `io.LimitReader` to prevent memory exhaustion from decompression bombs
</patterns>

<related>
gosec/G120, gosec/G304
</related>
