# staticcheck: SA5010

<instructions>
Detects impossible type assertions where the type being asserted cannot possibly match because the static type of the expression is already a concrete type that does not satisfy the asserted interface. The assertion will always fail at runtime.

Use a variable with the correct interface type, or remove the impossible assertion.
</instructions>

<examples>
## Good
```go
var r io.Reader = os.Stdin
if c, ok := r.(io.Closer); ok {
    c.Close()
}
```
</examples>

<patterns>
- Replace type assertions on concrete types with assertions on interface-typed variables
- Remove type assertions that always succeed or always fail
- Use interface types when type assertions are needed
- Switch on interface types instead of concrete types for meaningful type checks
</patterns>

<related>
staticcheck/SA4020, staticcheck/SA4023
</related>
