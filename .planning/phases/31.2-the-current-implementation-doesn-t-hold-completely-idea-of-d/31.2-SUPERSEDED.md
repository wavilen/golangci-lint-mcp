# Phase 31.2: Smart Flag Replacement — SUPERSEDED

**Status:** Superseded by Phases 40, 42, and 45
**Date marked:** 2026-04-22
**Marked by:** Phase 46 (Verification & Traceability Closure)

## Original Goal

Fix naive `--output.json.path stdout` injection to intelligently strip all conflicting output-format flags (text, tab, html, json, checkstyle, code-climate, junit-xml, teamcity, sarif, show-stats, color, verbose, plus legacy flags) before injecting JSON output flag — across plugin, command template, and rules files.

## Supersession Evidence

Phase 31.2 was never executed (only PLAN.md exists). Its goals were fully absorbed by three later phases:

1. **Phase 40** (strip pipe/redirect filters) — added `stripOutputFilters()` to strip `head`, `tail`, `grep` and other filter utilities from golangci-lint commands
2. **Phase 42** (precise command detection) — added `isGolangciLintCommand()` to match only actual golangci-lint invocations, not any Bash command containing the string
3. **Phase 45** (smart flag replacement + export fix) — added `injectJsonOutputFlag()` with `OUTPUT_FLAG_PATTERNS_VALUE` (11 patterns) and `OUTPUT_FLAG_PATTERNS_BOOL` (11 patterns) covering all output-format flag variants

### Must-Have Coverage Map

| 31.2 Must-Have | Absorbed By | Evidence |
|----------------|-------------|----------|
| Strip conflicting output flags (--output.text.*, --out-format, etc.) | Phase 45 → `injectJsonOutputFlag()` | `OUTPUT_FLAG_PATTERNS_VALUE` (11 patterns matching text, tab, html, checkstyle, code-climate, junit-xml, teamcity, sarif, json, out-format, color) + `OUTPUT_FLAG_PATTERNS_BOOL` (11 patterns matching print-linter-name, print-issued-lines, colors, show-stats, verbose, etc.) in `plugins/golangci-lint.js` lines 3-29 |
| Don't duplicate --output.json.path if already set | Phase 45 → `injectJsonOutputFlag()` | Token parser handles `--output.json.path` in OUTPUT_FLAG_PATTERNS_VALUE, strips it, then appends once. See line 127 function. |
| Preserve non-output flags | Phase 45 → `injectJsonOutputFlag()` | Token parser only strips matched patterns; all other tokens pass through to `kept[]` array. Verified by 93 passing tests. |
| Command template flag instructions | Phase 36 → `commands/golangci-lint.md` | Lines 22-33 contain explicit flag conflict documentation with full list of conflicting output flags and strip instruction. |
| Rules files flag replacement guidance | Phase 36 → rules files | Rules files instruct agents to strip conflicting output format flags before executing golangci-lint. |

All 5 must-haves from 31.2-01-PLAN.md are covered by completed work. No further action needed.
