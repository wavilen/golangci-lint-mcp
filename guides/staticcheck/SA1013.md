# staticcheck: SA1013

<instructions>
Detects `context.Value` calls where the return value is used without a type assertion. `context.Value` returns `interface{}` (or `any`), so accessing it without asserting the type defeats type safety and can cause nil pointer dereferences at runtime.

Always assert the type when retrieving values from context: `val, ok := ctx.Value(key).(string)`.
</instructions>

<examples>
## Good
```go
func handler(ctx context.Context) {
    id, ok := ctx.Value(requestIDKey).(string)
    if !ok {
        id = "unknown"
    }
    slog.Info("id", "value", id)
}
```
</examples>

<patterns>
- Assert the type from `context.Value` before assigning to a typed variable
- Check the `ok` boolean from the type assertion after calling `context.Value`
- Handle the case where the context key is missing or has an unexpected type
</patterns>

<related>
staticcheck/SA1012, staticcheck/SA1029
</related>
