# staticcheck: SA4017

<instructions>
Detects function calls whose return values are discarded when they shouldn't be. This includes calls to functions that return errors, functions with side effects that communicate results via return values, and pure functions whose entire purpose is the return value.

Assign the return value to a variable and handle it, or use the blank identifier `_` only when the discard is intentional and safe.
</instructions>

<examples>
## Bad
```go
os.Remove("temp.txt") // error ignored
```

## Good
```go
if err := os.Remove("temp.txt"); err != nil && !os.IsNotExist(err) {
    log.Printf("failed to remove temp file: %v", err)
}
```
</examples>

<patterns>
- Discarding error returns from I/O operations
- Ignoring return values from `fmt.Fprintf`, `io.Copy`, etc.
- Calling pure functions (like `strings.Contains`) without using the result
- Not checking `http.ResponseWriter.Write` return values
</patterns>

<related>
errcheck, SA4027
</related>
