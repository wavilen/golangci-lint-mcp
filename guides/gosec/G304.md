# G304

<instructions>
Detects file paths constructed from user-supplied input, enabling path traversal attacks. An attacker can use `../` sequences to read or write files outside the intended directory, accessing secrets, config files, or source code. Sanitize paths with `filepath.Clean` and verify the resolved path stays within the allowed base directory.
</instructions>

<examples>
## Good
```go
basePath := "/var/data/"
filename := r.URL.Query().Get("file")
fullPath := filepath.Clean(filepath.Join(basePath, filename))
if !strings.HasPrefix(fullPath, basePath) {
    http.Error(w, "invalid path", http.StatusBadRequest)
    return
}
data, err := os.ReadFile(fullPath)
```
</examples>

<patterns>
- Sanitize user input before joining with file paths — use `filepath.Join` and validate the result stays within the allowed root
- Validate `r.FormValue` against an allowlist or use `filepath.SafeJoin` before file system operations
- Check file paths from HTTP headers against a base directory — reject paths containing `..` or absolute paths
</patterns>

<related>
gosec/G305, gosec/G204, gosec/G107
</related>
