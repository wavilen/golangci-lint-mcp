# staticcheck: SA5009

<instructions>
Detects `fmt.Sprintf` calls where the result is only used as a format string for another print function, such as `fmt.Printf(fmt.Sprintf(...))`. This is redundant — the formatting can be done directly in the outer call.

Combine the formatting into a single call by using the outer function's format string directly.
</instructions>

<examples>
## Bad
```go
fmt.Printf(fmt.Sprintf("Hello %s", name))
```

## Good
```go
fmt.Printf("Hello %s", name)
```
</examples>

<patterns>
- `fmt.Printf(fmt.Sprintf(...))` — use `Printf` directly
- `fmt.Println(fmt.Sprintf(...))` — use `Printf` or `Println` directly
- `log.Printf(fmt.Sprintf(...))` — use `log.Printf` directly
- Building format strings dynamically when not needed
</patterns>

<related>
SA6006, SA4017
</related>
