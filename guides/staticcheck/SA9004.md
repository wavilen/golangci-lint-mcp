# staticcheck: SA9004

<instructions>
Detects const declarations with an unnecessary explicit type when the type can be inferred from the initializer. For example, `const x int64 = 5` is redundant because the compiler can infer the type. Removing the explicit type makes the constant more flexible — it becomes an untyped constant that can be used with any compatible type.

Remove the explicit type from the const declaration unless the type constraint is intentional.
</instructions>

<examples>
## Bad
```go
const maxSize int64 = 1024
const pi float64 = 3.14159
```

## Good
```go
const maxSize = 1024
const pi = 3.14159
```
</examples>

<patterns>
- Remove explicit types from const declarations where untyped constants suffice
- Remove type annotations like `const s string = "hello"` when the type is inferrable
- Remove unnecessary type constraints on constants used in mixed-type arithmetic
- Simplify package-level constants by removing redundant type annotations
</patterns>

<related>
SA9003, SA9002
</related>
