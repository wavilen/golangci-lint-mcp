# G119

<instructions>
Detects unsafe HTTP redirect policies that may propagate sensitive headers (Authorization, Cookie) across host boundaries. When `CheckRedirect` is customized or disabled, credentials intended for one host can leak to a different host during redirects.

Do not disable redirect checking. When customizing redirect policy, strip sensitive headers before following cross-host redirects.
</instructions>

<examples>
## Good
```go
client := &http.Client{
    CheckRedirect: func(req *http.Request, via []*http.Request) error {
        if len(via) >= 10 {
            return fmt.Errorf("too many redirects")
        }
        if req.URL.Host != via[0].URL.Host {
            req.Header.Del("Authorization")
            req.Header.Del("Cookie")
        }
        return nil
    },
}
```
</examples>

<patterns>
- Configure `CheckRedirect` to validate redirect targets — limit redirect count and check host allowlists
- Validate redirect destination hosts against an allowlist before forwarding headers
- Replace manual redirect loops with a configured `CheckRedirect` policy that limits hops and validates targets
</patterns>

<related>
gosec/G107, gosec/G113
</related>
