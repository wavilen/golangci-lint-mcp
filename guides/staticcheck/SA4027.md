# staticcheck: SA4027

<instructions>
Detects calls to `net.Conn.Write` (or `WriteTo`, `ReadFrom`) where the return value — specifically the error — is not checked. Network writes can fail silently if the error is ignored, leading to data loss or incomplete communication.

Check the error return value. At minimum, log the error. For critical writes, retry or propagate the error.
</instructions>

<examples>
## Good
```go
if _, err := conn.Write(data); err != nil {
    return errors.Wrap(err, "write failed")
}
```
</examples>

<patterns>
- Check the error return from `conn.Write(data)` — network writes can fail silently
- Check return values from `conn.WriteTo` and `conn.ReadFrom` for errors
- Handle HTTP response write errors instead of ignoring them
- Check file write errors in network handlers
</patterns>

<related>
errcheck, staticcheck/SA4017
</related>
